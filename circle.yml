machine:
  node:
    version: 6
  timezone:
    America/Denver

  environment:
    CIRCLE_ENV: test
    NODE_ENV: test

dependencies:
  cache_directories:
    - node_modules
  override:
    # go npm
    - npm install
test:
  override:
    - npm run test
