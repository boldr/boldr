machine:
  node:
    version: 6
  timezone:
    America/Denver

  environment:
    NODE_ENV: test

general:
  artifacts:
    - "docs/reports"

dependencies:
  cache_directories:
    - node_modules
  override:
    # go npm
    - npm install
test:
  override:
    - npm run test
